# Raw sources

Drop immutable source documents here (articles, clips, exports). The LLM reads from this folder but never modifies files.

Suggested layout:

- `articles/` — web clips (Obsidian Web Clipper)
- `assets/` — downloaded images for clipped articles
- `papers/` — PDFs or markdown notes
